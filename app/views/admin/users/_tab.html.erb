<%# locals: (user:) %>

<div class="mb-3">
  <%= link_to '« Back', admin_users_path %>
</div>

<h1 class="mb-3"><%= user.id %></h1>

<ul class="nav nav-tabs mb-3">
  <li class="nav-item">
    <%= link_to 'Profile', edit_admin_user_path(user), class: ['nav-link', active: controller_path == 'admin/users'] %>
  </li>

  <li class="nav-item">
    <%= link_to 'SSH Keys', admin_user_ssh_keys_path(user), class: ['nav-link', active: controller_path == 'admin/ssh_keys'] %>
  </li>
</ul>
