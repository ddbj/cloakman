<div class="mb-3">
  <%= link_to "« Back", ssh_keys_path %>
</div>

<h1 class="mb-3">Add new SSH key</h1>

<%= bootstrap_form_with model: @form, scope: :form, url: ssh_keys_path do |f| %>
  <%= f.text_area :ssh_key, label: "Key", required: true, data: { controller: "autosize" }, help: "Supported key types: RSA, ECDSA, and ED25519." %>

  <%= f.primary "Add SSH key" %>
<% end %>
